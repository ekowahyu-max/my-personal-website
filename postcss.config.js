export default {
    plugins: {
      // Memastikan Tailwind CSS dijalankan sebagai plugin PostCSS
      tailwindcss: {},
      // Memastikan prefixer (penting untuk kompatibilitas browser) juga dijalankan
      autoprefixer: {},
    },
  }